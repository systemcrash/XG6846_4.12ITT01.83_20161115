XG6846_ITT Consumer Release Notes

This release contains all of the files needed to build a XG6846_ITT
image.  To build an image, enter:

# make PROFILE=<profile_name>

For example,

# make PROFILE=XG6846_ITT

The final image is located in the images subdirectory.  This image can be
specified in the XG6846_ITT Web interface image upgrade item in order to
upgrade the built image.

